# Netlify redirects for API proxy
# This proxies all /api/* requests to the backend server
/api/*  https://yikiat.com/api/:splat  200

# SPA fallback - serve index.html for all routes not matched above
/*      /index.html   200